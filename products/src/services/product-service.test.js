// Which service we are testing
describe("Product service", () => {
    // which function we are testing
    describe("Create product function", () => {
        // which case we are testing
        test("should case...", () => {});
        test("should case...", () => {});
    });
});
